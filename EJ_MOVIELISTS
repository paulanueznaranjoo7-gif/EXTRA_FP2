class Movie:
    def __init__(self, title, year):
        self.title = title
        self.year = year

    def __str__(self):
        return f"{self.title} ({self.year})"


class MovieList:

    class Node:
        def __init__(self, value, next=None):
            self.value = value
            self.next = next

    def __init__(self):
        self.first = None
        self.last = None

    def __str__(self):
        current = self.first
        text = ""

        while current is not None:
            text += f"[{current.value}] -> "
            current = current.next

        return text + "None"

    def add_first(self, movie):
        new_node = self.Node(movie, self.first)

        if self.first is None:
            self.last = new_node

        self.first = new_node

    def remove_last(self):
        if self.first is None:
            return 
